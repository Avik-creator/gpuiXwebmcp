# gpuiXwebmcp site

Explainer for the native WebMCP debugger. Palette matches the GPUI window (`#0F172A`, `#1B2336`, `#22C55E`).

```sh
cd site
npm install
npm run dev
```

Open [http://localhost:3000/](http://localhost:3000/). Routes: `/`, `/webmcp`, `/gpui`.
